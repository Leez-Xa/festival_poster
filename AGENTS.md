# AGENTS.md

本文件是所有参与本项目的 agent 必须先阅读并遵守的公共协作与文件安全规则。当前项目处于迭代开发阶段，不再以旧 MVP 范围作为功能上限。

## 项目目录

项目根目录：

```text
D:\Codex_Projects\festival_poster
```

## 重要文件和素材目录

以下文件和目录默认视为重要资产，除非用户明确要求，否则只读：

```text
D:\Codex_Projects\festival_poster\节日节气海报宣传AI员工_PRD.md
D:\Codex_Projects\festival_poster\素材
D:\Codex_Projects\festival_poster\.env
D:\Codex_Projects\festival_poster\festival_poster_主要程序复印件_20260607.zip
D:\Codex_Projects\festival_poster\festival_poster_主要程序复印件_20260607
```

`素材` 目录包含但不限于：

```text
D:\Codex_Projects\festival_poster\素材\asset
D:\Codex_Projects\festival_poster\素材\二维码汇总
D:\Codex_Projects\festival_poster\素材\产品资料
D:\Codex_Projects\festival_poster\素材\朴道logo
D:\Codex_Projects\festival_poster\素材\节日节气海报
```

旧版根目录下的 `asset`、`二维码汇总`、`朴道logo`、`节日节气海报` 等路径已迁移到 `素材` 目录下；查找素材时应优先使用上述新路径。

不允许删除、移动、重命名、覆盖或批量改写上述文件和目录。可以读取这些资产用于索引、匹配、提示词构建、生成测试和只读分析。

## 禁止行为

1. 禁止执行删除类命令，包括但不限于 `rm`、`del`、`Remove-Item`、`rmdir`、`git clean`。
2. 禁止执行破坏性 Git 命令，包括但不限于 `git reset --hard`、`git checkout --`、`git restore`、`git clean`、force push。
3. 禁止移动、重命名、覆盖现有素材文件和备份文件。
4. 禁止修改 PRD，除非用户明确要求。
5. 禁止修改系统目录、用户目录、全局环境变量、全局 npm/pip 配置。
6. 禁止把真实 API Key 写进代码、日志、README、提交记录或示例文件。
7. 禁止打印 `.env` 中的真实 Key。
8. 禁止为了实现新功能破坏现有手动生成流程。

## 允许行为

1. 可以读取素材、PRD、README、现有代码和本地配置示例。
2. 可以围绕迭代目标新增或修改 `app/`、`frontend/`、`config/`、`scripts/`、`docs/` 中的代码和文档。
3. 可以创建或维护 `.env.example`，但不能写入真实 Key。
4. 可以创建本地 `storage/`、`tmp/`、`logs/` 等运行目录。
5. 可以安装项目本地依赖，但不要全局安装。
6. 可以扩展接口、页面、测试和提示词逻辑，前提是保留已有手动选择流程可用。
7. 修改代码前必须先读取相关文件，并尽量遵循现有 FastAPI、静态前端和任务轮询结构。

## 迭代开发方向

以下方向是当前优先迭代参考，不构成功能范围上限；如旧 MVP 描述与用户最新迭代目标冲突，以用户最新明确指令为准：

1. 一句话一键生图：用户输入自然语言指令，系统自动解析节日活动、产品、风格、画面元素和文案方向。
2. 自动素材匹配：优先从 `素材\产品资料`、Logo、二维码、底部宣传条和参考海报中匹配可用资产。
3. 提示词优化：将用户指令、产品卖点、品牌资产、节日氛围和负向约束整理为更精准的生图提示词。
4. 整图融合：优先让图片模型理解并融合产品、Logo、底部宣传条和整体画面风格，减少机械拼贴感。
5. 二维码保真：二维码必须尽量保持可扫码；需要时可采用模型预留区域加后处理校验和保真替换。
6. 双模式入口：保留现有手动配置流程，同时新增一键生成页面或入口，供用户按场景选择。
7. 测试与验收：新增后端接口、前端交互、提示词生成和任务轮询时，应补充相应检查或脚本。

## 需要先征得用户确认的操作

如果确实需要删除、移动、覆盖、重命名、清理文件，必须先停止并向用户说明：

1. 要操作的文件路径。
2. 为什么必须这么做。
3. 是否有替代方案。
4. 操作风险。

没有用户明确确认，不要执行任何破坏性操作。

## 多 Agent 协作规则

1. 主会话负责产品方向、任务拆分、最终整合和验收。
2. 子会话应只修改自己负责的模块，避免跨模块抢改同一文件。
3. 子会话开始前必须读取本文件和相关代码；完成后必须汇报改动文件、验证命令和剩余风险。
4. 后端、前端、提示词、测试可以并行推进，但接口字段、任务状态和用户体验要由主会话统一收口。
5. 如发现其他 agent 或用户已有未提交改动，不要回滚；先读取并在其基础上继续，必要时向主会话说明冲突。
